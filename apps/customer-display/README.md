# ServeOS Customer Display

**Status:** Scaffold only — not a shipping UI yet.

## Experience

Front-of-house screen guests can see:

- “Now serving” / ready order numbers
- Optional preparing queue
- Optional promos (later)

Not a kitchen tool — that is `apps/kds`.

## Product phase

Phase 3 / Future in `docs/fullSaaSProductDetails.md` (“Customer Display Screen”).

## Related today

- Devices workspace stub in venue admin
- Hardware kind `customerStatus` in deployment config

## Build later against

- Order status events (public-safe projection only)
- Paired display device identity per venue / location

See `docs/productExperiences.md`.

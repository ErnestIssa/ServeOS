/** Payments loading UI — re-exports shared config loading primitives (Overview style). */
export {
  ConfigSectionSpinner as PaymentsSectionSpinner,
  ConfigDrawerSpinner as PaymentsDrawerSpinner,
  ConfigDetailsReveal as PaymentsDetailsReveal,
  ConfigModalContentGate as PaymentsModalContentGate,
  ConfigBusyLabel as PaymentsBusyLabel
} from "../configLoadingUi";

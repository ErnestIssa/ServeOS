# Optional: call the hosted API from Vite dev (no local backend on :3000)
# If unset, requests go to the Vite dev server and are proxied to http://127.0.0.1:3000
# (run: npm run dev:backend from repo root)
# VITE_API_URL=https://serveos-api.onrender.com

# Demo menu bundle (offline UI): fills customer Home with "Peak Mode Bistro" + categories.
# Deployment wiring only — optional local API override (production default is Render).
# EXPO_PUBLIC_API_URL=http://127.0.0.1:3000

# Dev-only demo menu UI (not production service config)
EXPO_PUBLIC_SERVEOS_USE_DEMO_MENU=false

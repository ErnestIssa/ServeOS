/** Re-export shim — types live in `profilePrefsStorage.ts` for Metro resolution. */
export type {
  AppSettings,
  ProfileStackRoute,
  SettingsDetailKey
} from "./profilePrefsStorage";
export { DEFAULT_APP_SETTINGS } from "./profilePrefsStorage";
